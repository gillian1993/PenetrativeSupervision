import fs from 'node:fs'
import path from 'node:path'

const root = process.cwd()
const sourcePath = path.join(root, 'outputs', '穿透式监管产品PRD-v11.14-核心业务流程图重绘版.md')
const targetPath = path.join(root, 'outputs', '穿透式监管产品PRD-v11.15-功能介绍语句版.md')

let content = fs.readFileSync(sourcePath, 'utf8')

content = content
  .replace('# 穿透式监管智能应用平台 PRD（v11.14 核心业务流程图重绘版）', '# 穿透式监管智能应用平台 PRD（v11.15 功能介绍语句版）')
  .replace('| 当前版本 | v11.14 核心业务流程图重绘版 |', '| 当前版本 | v11.15 功能介绍语句版 |')

const changeLogHeader = '| --- | --- | --- | --- | --- | --- |'
const introChangeRow = '| 2026-07-27 | 将10.3各功能节点的功能介绍由表格改为自然段说明，并清理误插入业务表格的版本记录行 | 提升PRD可读性，避免功能介绍与字段表、操作表混用 | [TODO] | [TODO] | v11.15 |'
content = content.replace(changeLogHeader, `${changeLogHeader}\n${introChangeRow}`)

const badVersionText = '重新绘制核心业务流程图，按知识图谱准备、监管能力配置、运行触发与预警研判、风险事件整改复核和效果回流表达端到端闭环'
let seenV1114 = false
content = content.split(/\r?\n/).filter((line) => {
  if (!line.includes(badVersionText)) return true
  if (!seenV1114 && line.includes('| v11.14 |')) {
    seenV1114 = true
    return true
  }
  return false
}).join('\n')

const introBlocks = new Map([
  ['10.3.1.1 功能介绍', `监管工作台是大多数授权用户登录后的默认工作入口，核心目标是集中回答“我现在需要处理什么”。它面向监管负责人、审计纪检人员、领域监管专员、业务责任人及管理类用户，在用户登录、点击一级菜单或从其他业务页面返回时触发使用。

页面基于当前用户、角色、组织权限、待办任务、业务对象状态和业务消息加载数据，输出统计筛选上下文、来源对象跳转、待办处理结果和消息已读状态。工作台不承载全量风险分析，而是作为个人监管任务聚合和处置起点。`],
  ['10.3.2.1 功能介绍', `监管态势为集团领导、监管负责人、审计纪检人员和领域监管专员提供全局风险观察入口，用于快速掌握授权范围内的风险规模、等级结构、处置进度和重点异常。

用户点击“监管态势”一级菜单，或纯查看角色按配置进入页面后，系统按照权限范围汇总预警、风险事件、核查整改状态、组织和领域维度数据，输出核心指标、趋势分布、重点事项，以及带筛选条件的下钻链接。`],
  ['10.3.3.1 功能介绍', `风险监管在同一能力域内承接预警研判、核查复核和确认风险后的整改关闭。它面向监管负责人、审计纪检人员、领域监管专员和业务责任人，使用预警与风险事件两个独立业务对象保持编号、状态和追溯关系清晰。

系统以已发布场景规则、图谱事实、证据快照、研判结论和整改材料作为输入，输出预警处置结论、风险事件、整改复核记录、审计记录和后台效果样本。预警关注“是否需要升级”，风险事件关注“如何整改并关闭”。`],
  ['10.3.4.1 功能介绍', `场景与规则以风险场景版本为统一发布单元，编排确定性标准规则和智能Skill两类检测能力，形成可校验、可试跑、可追溯的风险识别配置。当前工程在一级菜单“场景与规则”下提供“风险场景、规则管理、Skill管理”三个二级入口，活动路由分别为\`/scenes\`、\`/rules\`、\`/skills\`。

场景负责风险主题、监管领域、默认风险等级、适用组织、例外范围和核查模板，不再在场景基本信息中直接配置本体、图谱、主对象或目标类。标准规则负责基于图谱和本体元素的确定性判断；智能Skill根据审查需求生成审查内容、审查要求和输出结果，并在运行时自动获取场景业务上下文和关联附件。

规则和Skill均为独立资产，通过版本引用与一个或多个场景版本建立多对多关系；移出场景只解除引用，不删除资产。场景状态包括草稿、待试跑、待发布、已发布和已停用；已发布或已停用场景只读。当前活动页面不提供规则或Skill独立发布、复制新版本和单独试跑入口，规则和Skill随场景发布被锁定为已发布版本。`],
  ['10.3.5.1 功能介绍', `本体与图谱章节描述平台如何用可版本化的语义模型连接来源数据，形成可被规则运行和风险证据引用的图谱版本。当前工程入口以“知识图谱”产品命名呈现，包含“图谱结构”和“数据源”两个主入口；旧的“数据接入”路由会自动跳转至“图谱管理-数据源”页签。

当前模型只维护类、属性、关系三类结构元素，业务对象、业务记录和发生事项均统一建模为本体类。中标确认、合同变更、采购立项等发生语义不再使用独立事件元素，而是使用带主标识、发生时间属性及参与对象关系的类实例表达。

公共语义映射统一为节点实例和关系两类，节点实例可映射到本体类或属性，关系只能映射到本体关系。图谱版本统一统计节点和关系，原事件实例及事件数量迁移并入节点口径，不再单列事件数量。本体、数据源、公共映射、图谱版本和依赖快照使用Node.js + MySQL接口；本体编辑模式和画布位置保存在浏览器本地偏好中。`],
  ['10.3.6.1 功能介绍', `系统管理为平台提供用户组织、角色权限和审计追溯能力，主要面向系统管理员、安全管理员和审计人员。

系统以IAM组织用户、角色授权、敏感等级和业务操作日志作为输入，输出有效账号、权限策略和只读审计记录。该模块用于支撑菜单访问、数据范围控制、高危操作授权和关键操作留痕，是监管业务上线运行的基础治理能力。`],
])

for (const [heading, prose] of introBlocks.entries()) {
  const pattern = new RegExp(`(##### ${heading}\\n\\n)[\\s\\S]*?(?=\\n##### ${heading.replace('.1 功能介绍', '.2 功能清单')})`)
  if (!pattern.test(content)) throw new Error(`未找到小节: ${heading}`)
  content = content.replace(pattern, `$1${prose}\n\n`)
}

fs.writeFileSync(targetPath, content, 'utf8')
console.log(targetPath)
