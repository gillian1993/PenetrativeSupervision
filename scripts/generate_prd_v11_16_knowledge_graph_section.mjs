import fs from 'node:fs'
import path from 'node:path'

const root = process.cwd()
const sourcePath = path.join(root, 'outputs', '穿透式监管产品PRD-v11.15-功能介绍语句版.md')
const targetPath = path.join(root, 'outputs', '穿透式监管产品PRD-v11.16-知识图谱章节工程对齐版.md')

let content = fs.readFileSync(sourcePath, 'utf8')

content = content
  .replace('# 穿透式监管智能应用平台 PRD（v11.15 功能介绍语句版）', '# 穿透式监管智能应用平台 PRD（v11.16 知识图谱章节工程对齐版）')
  .replace('| 当前版本 | v11.15 功能介绍语句版 |', '| 当前版本 | v11.16 知识图谱章节工程对齐版 |')

const changeLogHeader = '| --- | --- | --- | --- | --- | --- |'
const changeLogRow = '| 2026-07-27 | 按当前工程重写10.3.5知识图谱章节，统一采用图谱结构、数据源、字段映射和知识图谱生成等产品概念 | 当前页面已不再面向用户呈现旧语义资产概念，需与工程导航、页面文案和交互边界保持一致 | [TODO] | [TODO] | v11.16 |'
content = content.replace(changeLogHeader, `${changeLogHeader}\n${changeLogRow}`)

const newSection = `#### 10.3.5 知识图谱

##### 10.3.5.1 功能介绍

知识图谱模块用于维护监管分析所需的图谱结构、数据源接入、字段映射和知识图谱生成结果。当前工程在一级菜单“知识图谱”下提供“图谱结构”和“数据源”两个二级入口，活动路由分别为\`/graphs/structures\`和\`/graphs/sources\`；知识图谱生成向导保留独立路由，用于选择图谱结构和数据源后生成可发布的知识图谱。

图谱结构负责定义类、属性和关系，作为字段映射、知识图谱生成和规则配置的统一语义边界。数据源负责登记来源系统、解析来源结构，并维护来源字段到图谱字段的映射。知识图谱生成过程会固化图谱结构、数据源集合和字段映射快照，发布后的知识图谱可被规则资产引用，用于证据关联和风险穿透分析。

当前工程不提供独立事件建模入口，也不在产品页面中单独维护事件数量。采购立项、中标确认、合同变更等业务发生记录通过“类 + 发生时间属性 + 关联关系”表达，并在生成知识图谱时作为节点及关系进入图谱。

**功能流程图**

![知识图谱功能流程图](./assets/flows/knowledge-graph-function-flow-v11.16.svg)

> 图10-8 知识图谱功能流程图。按当前工程展示图谱结构、数据源、字段映射和知识图谱生成发布流程。

##### 10.3.5.2 功能清单

| 功能节点 | 页面/入口 | 功能说明 | 优先级 |
| --- | --- | --- | --- |
| 图谱结构列表 | 一级菜单“知识图谱” → 二级菜单“图谱结构” | 查询、新建、校验、发布、复制版本和删除图谱结构草稿 | P0 |
| 图谱结构编辑器 | 图谱结构列表 → 编辑/查看 | 维护基本信息、类、属性、关系和版本记录；支持结构化配置和可视化建模 | P0 |
| 数据源列表 | 一级菜单“知识图谱” → 二级菜单“数据源” | 查询数据源、查看统计指标、新建数据源、测试连接、启用/停用和删除 | P0 |
| 数据源详情 | 数据源列表 → 进入详情 | 查看基本配置、解析来源结构、维护字段映射、校验字段映射和切换启停状态 | P0 |
| 字段映射 | 数据源详情 → 字段映射页签 | 以图谱字段为主线，将来源字段映射到图谱字段，并保存转换与识别规则 | P0 |
| 知识图谱生成 | 独立生成向导 | 选择图谱结构和数据源，检查字段映射状态，生成或重新生成知识图谱 | P1 |

##### 10.3.5.3 功能详细设计

**（一）图谱结构列表**

**1. 页面布局**

1. 用户进入“知识图谱 / 图谱结构”后，页面头部展示标题、说明、“刷新”和“新建图谱结构”。
2. 筛选区包含关键词和状态，关键词匹配结构名称、结构编码或监管领域。
3. 列表展示结构名称/编码、范围/领域、版本、类/属性/关系数量、状态、更新时间和操作。
4. 新建图谱结构、校验、发布、复制版本和删除草稿均通过弹窗完成确认。
5. 已发布版本只读，变更必须复制新版本。

查询条件：

| 字段 | 控件 | 默认值 | 规则 |
| --- | --- | --- | --- |
| 关键词 | 输入框 | 空 | 匹配结构名称、结构编码或监管领域 |
| 状态 | 单选下拉 | 全部 | 全部、草稿、待校验、已发布、已废止 |

列表字段：

| 字段 | 展示规则 |
| --- | --- |
| 结构名称/编码 | 名称为详情入口，编码为稳定标识 |
| 范围/领域 | 基础图谱结构或领域图谱结构，并展示监管领域 |
| 版本 | 展示当前版本号 |
| 类/属性/关系 | 只统计三类结构元素，不展示事件数量 |
| 状态 | 草稿、待校验、已发布、已废止 |
| 更新时间 | 展示最近更新时间 |
| 操作 | 编辑/查看、校验、发布、复制版本、删除；按状态动态显示 |

新建图谱结构字段：

| 字段 | 必填 | 控件 | 规则 |
| --- | --- | --- | --- |
| 结构名称 | 是 | 输入框 | 不得为空 |
| 结构编码 | 是 | 输入框 | 3至64位，仅允许大写字母、数字、下划线和中划线，全局唯一 |
| 适用范围 | 否 | 下拉 | 基础图谱结构、领域图谱结构 |
| 监管领域 | 否 | 下拉 | 采购、合同、财务、投资、通用 |
| 结构说明 | 否 | 多行输入 | 说明覆盖的业务对象、业务记录和边界 |

**2. 操作规则**

| 操作 | 前置条件 | 成功结果 | 失败处理 |
| --- | --- | --- | --- |
| 查询/重置 | 页面已加载 | 按条件刷新列表或恢复默认列表 | 加载失败保留筛选条件 |
| 新建图谱结构 | 名称和编码合法 | 创建草稿并进入编辑器 | 编码重复、格式错误或名称为空时停留弹窗 |
| 校验 | 草稿版本 | 无阻断项时进入待校验 | 有阻断项时保持草稿并展示原因 |
| 发布 | 待校验版本 | 发布后版本只读 | 未校验或检查不通过时拒绝 |
| 复制版本 | 已发布版本 | 创建新草稿并进入编辑器 | 源版本不存在时提示失败 |
| 删除 | 未发布且未被引用 | 删除当前草稿 | 已发布或被规则、图谱引用时拒绝 |

**（二）图谱结构编辑器**

**1. 页面布局**

1. 页面头部展示返回图谱结构、结构编码、名称、状态、版本、范围和最近保存时间。
2. 页面提供“结构化配置”和“可视化建模”两种编辑方式，系统记忆当前结构最近使用的编辑方式。
3. 结构化配置包含基本信息、类定义、属性、关系、版本记录页签。
4. 可视化建模提供新增类、新增属性、新增关系、布局方式、重新布局和缩放控制。
5. 草稿或待校验版本展示保存草稿、校验和发布；已发布版本仅展示复制新版本。

基本信息字段：

| 字段 | 必填 | 控件 | 规则 |
| --- | --- | --- | --- |
| 结构名称 | 是 | 输入框 | 不得为空；已发布只读 |
| 结构编码 | 是 | 只读输入 | 创建后不可修改 |
| 适用范围 | 是 | 下拉 | 基础图谱结构、领域图谱结构 |
| 监管领域 | 是 | 下拉 | 通用、采购、合同、财务、投资 |
| 结构说明 | 否 | 多行输入 | 描述覆盖范围和使用边界 |
| 版本统计 | 系统生成 | 摘要面板 | 展示类、属性和关系数量 |

结构元素字段：

| 元素 | 字段 | 规则 |
| --- | --- | --- |
| 类 | 元素编码、中文名称、约束、业务说明 | 编码在当前结构内唯一；可表达业务对象、业务记录或业务概念 |
| 属性 | 属性编码、中文名称、所属类、数据类型、约束、业务说明 | 必须选择当前结构中存在的类 |
| 关系 | 关系编码、中文名称、起点类、终点类、关系方向、约束、业务说明 | 起点类和终点类必须属于当前结构；方向由系统生成 |

业务发生记录表达规则：

| 业务语义 | 当前表达方式 | 示例 |
| --- | --- | --- |
| 业务发生记录 | 建模为普通类 | 中标确认、合同变更、采购立项 |
| 记录主标识 | 在该类下配置主标识属性 | 中标确认记录编号 |
| 发生时间 | 在该类下配置日期时间属性并标记约束 | 中标确认发生时间 |
| 参与对象 | 使用关系连接业务记录类与业务对象类 | 中标确认 → 采购项目 |
| 图谱落地 | 生成知识图谱时形成节点和关系 | 来源记录生成节点，并关联采购项目节点 |

校验与发布规则：

1. 图谱结构至少包含一个类。
2. 所有结构元素编码和名称不能为空，同类型编码不得重复。
3. 属性必须引用当前结构中存在的所属类。
4. 关系必须同时配置有效的起点类和终点类。
5. 修改或删除被属性、关系引用的类时，系统返回引用冲突并要求先调整依赖。
6. 未配置主标识属性时产生提示项，不直接阻断校验。
7. 校验无阻断项后状态进入待校验；只有待校验版本可以发布。
8. 发布后结构和元素只读，修改必须复制新版本。

**（三）数据源列表**

**1. 页面布局**

1. 用户进入“知识图谱 / 数据源”后，页面展示关键词、状态筛选和“新建数据源”。
2. 页面展示数据源总数、启用数据源、异常数据源、近期处理数据4项统计指标。
3. 数据源列表展示来源名称/编码、适用图谱结构、来源方式、数据范围、责任人、最近成功、状态和操作。
4. 新建数据源使用抽屉，删除数据源使用高危确认弹窗。

列表字段：

| 字段 | 展示规则 |
| --- | --- |
| 来源名称/编码 | 名称为详情入口 |
| 适用图谱结构 | 展示该数据源关联的图谱结构 |
| 来源方式 | 数据库视图、API、消息、批量文件 |
| 数据范围 | 来源对象、时间和组织范围说明 |
| 责任人 | 当前数据源负责人 |
| 最近成功 | 最近一次成功测试或同步时间 |
| 状态 | 草稿、启用、停用、异常 |
| 操作 | 进入详情、测试、启用/停用、删除 |

新建数据源字段：

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| 适用图谱结构 | 是 | 只能选择已发布图谱结构 |
| 来源系统名称 | 是 | 不得为空 |
| 来源方式 | 是 | 数据库视图、API、消息、批量文件 |
| 责任人 | 是 | 当前提供张海、陈洁候选项 |
| 数据范围 | 是 | 说明对象、组织和时间范围 |

删除约束：

1. 启用中的数据源不可直接删除，需先停用。
2. 已被知识图谱生成结果引用的数据源不可删除。
3. 已存在同步记录的数据源不可删除。
4. 删除成功时级联清理该数据源的元数据和字段映射草稿。

**（四）数据源详情与字段映射**

**1. 页面布局**

1. 页面头部展示返回数据源列表、数据源编号、名称、状态、来源方式、适用结构、责任人、最近成功时间，以及测试连接和启用/停用按钮。
2. 顶部统计展示元数据对象、图谱字段、字段映射、必填覆盖4项指标。
3. 主体页签为基本配置和字段映射。
4. 基本配置展示来源基本信息和来源结构解析入口。
5. 字段映射以当前图谱结构字段为主线，展示字段要求、来源字段、转换规则、状态和操作。

字段映射规则：

1. 用户点击“解析来源结构”后，系统读取来源表、接口或消息结构，并生成字段映射建议。
2. 字段映射只处理来源字段到图谱字段的对应关系。
3. 用户点击图谱字段行“配置/编辑”，在抽屉中选择来源字段并填写转换与识别规则。
4. 字段映射保存后状态变为待校验。
5. 用户点击“校验字段映射”，系统校验来源字段、目标图谱字段和转换规则是否有效。
6. 至少存在有效字段映射后，数据源才允许启用。
7. 实例之间的关系在生成知识图谱时根据图谱结构和业务键自动构建，不在当前字段映射页单独维护关系页签。

异常处理：

| 异常 | 页面表现 | 恢复方式 |
| --- | --- | --- |
| 数据源不存在 | 展示数据源不可访问 | 返回数据源列表重新选择 |
| 当前结构无图谱字段 | 展示暂无图谱字段 | 先在图谱结构中维护属性字段 |
| 尚未解析来源结构 | 展示解析入口 | 点击解析来源结构并生成建议 |
| 来源字段未选择 | 不保存字段映射 | 选择来源字段后重试 |
| 转换规则为空 | 不保存字段映射 | 填写转换与识别规则 |
| 无有效字段映射 | 启用按钮禁用或接口拒绝 | 完成字段映射校验 |

**（五）知识图谱生成向导**

知识图谱生成向导用于生成或重新生成知识图谱结果。当前工程保留独立路由\`/graphs/new\`和\`/graphs/:id/edit\`，但未作为“知识图谱”菜单下的独立二级菜单展开。

向导步骤：

| 步骤 | 页面内容 | 下一步条件 |
| --- | --- | --- |
| 1. 基本信息 | 图谱名称、图谱编码、数据范围 | 名称和范围非空；编码格式有效 |
| 2. 图谱结构 | 选择已发布图谱结构，展示类、属性、关系数量 | 必须选择一个已发布图谱结构 |
| 3. 数据源与字段映射 | 选择启用数据源，自动检查映射模板状态 | 至少选择一个启用数据源，且所选数据源映射均就绪 |
| 4. 确认生成 | 核对图谱、图谱结构、数据源、映射模板快照和范围 | 用户确认生成或重新生成 |

依赖状态：

| 状态 | 含义 | 处理方式 |
| --- | --- | --- |
| ready | 数据源启用，映射存在且已校验 | 允许继续 |
| disabled | 数据源未启用 | 返回数据源详情启用 |
| missing | 当前数据源缺少字段映射 | 生成映射建议 |
| pending | 映射修订尚未校验或有效数量不足 | 进入维护并保存校验 |
| invalid | 目标字段不存在或映射类型不匹配 | 修正目标后重新校验 |

生成规则：

1. 生成知识图谱必须选择一个已发布图谱结构和至少一个启用数据源。
2. 所选数据源的字段映射状态必须全部就绪。
3. 生成时保存图谱结构、数据源、字段映射修订和完整快照。
4. 新建成功后形成待发布结果；重新生成会覆盖未发布结果的来源、范围和映射快照。
5. 发布后的知识图谱可被规则资产选择使用。
6. 规则已引用的已发布知识图谱不得被删除或覆盖。

**（六）业务规则与验收要求**

业务规则：

| 编号 | 类型 | 规则 |
| --- | --- | --- |
| KG-R1 | 版本约束 | 已发布图谱结构只读，修改必须复制新版本 |
| KG-R2 | 元素约束 | 图谱结构只维护类、属性和关系，不提供独立事件建模入口 |
| KG-R3 | 引用约束 | 被属性或关系引用的类不可直接改码或删除 |
| KG-R4 | 数据源约束 | 新建数据源必须选择已发布图谱结构 |
| KG-R5 | 映射约束 | 字段映射以图谱字段为主线，来源字段和转换规则必填 |
| KG-R6 | 启用约束 | 数据源启用前必须至少存在有效字段映射 |
| KG-R7 | 生成约束 | 知识图谱生成前必须选择已发布图谱结构、启用数据源和就绪映射 |
| KG-R8 | 快照约束 | 生成结果必须固化图谱结构、数据源集合和字段映射快照 |
| KG-R9 | 统计口径 | 知识图谱结果只展示节点和关系数量，不单列事件数量 |
| KG-R10 | 删除约束 | 数据源、图谱结构和知识图谱结果被引用时不可删除 |

验收要求：

1. 10.3.5章节及对应流程图均采用“知识图谱、图谱结构、数据源、字段映射、知识图谱生成”等产品概念。
2. 图谱结构列表和编辑器不出现独立事件数量、事件页签或新增事件入口。
3. 图谱结构编辑器可维护类、属性、关系，并能在结构化配置和可视化建模之间切换。
4. 数据源新建时必须选择已发布图谱结构，名称和数据范围为空时不允许创建。
5. 数据源详情支持解析来源结构、配置字段映射、校验字段映射和启用/停用。
6. 无有效字段映射时，数据源不可启用。
7. 知识图谱生成向导只能选择已发布图谱结构和启用数据源。
8. 字段映射缺失、待校验或无效时，知识图谱生成不能进入确认生成。
9. 发布后的知识图谱可被规则资产引用，并保持版本可追溯。

##### 10.3.5.4 应用操作

以下应用操作按当前工程活动路由、可见页面、真实字段、按钮、状态机和接口边界编写；本章节不再使用旧语义资产命名。

| 功能名称 | 位置/用户路径 | 原型 | 产品逻辑（页面结构/交互/策略设计） | 对应文案 | 优先级 | 评审意见 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 图谱结构列表 | 一级菜单“知识图谱” → 二级菜单“图谱结构” | [图片] | <strong>页面结构：</strong><br>页面头部展示“图谱结构”标题、说明、刷新和新建入口。筛选区包含关键词和状态；列表展示结构名称/编码、范围/领域、版本、类/属性/关系数量、状态、更新时间和操作。 图1<br>新建、校验、发布、复制版本和删除均使用弹窗。 图2<br><br><strong>交互逻辑：</strong><br>1. 用户进入图谱结构页面，系统展示已加载的图谱结构版本。<br>2. 用户按关键词或状态查询；点击“重置”恢复全部状态和空关键词。 图1<br>3. 用户点击“新建图谱结构”，填写结构名称、结构编码、适用范围、监管领域和结构说明，点击“创建并编辑”后进入草稿编辑器。 图2<br>4. 用户点击名称或“编辑/查看”进入图谱结构编辑器；草稿可编辑，已发布只读。 图3<br>5. 草稿点击“校验”，系统检查类、属性、关系、编码和引用完整性；无阻断项后进入待校验。 图4<br>6. 待校验版本点击“发布”，确认发布检查清单后变为已发布；已发布版本点击“复制版本”生成下一版本草稿。 图5<br>7. 非已发布版本可点击“删除”，系统校验未被场景、规则或图谱引用后删除。 图6<br><br><strong>策略设计：</strong><br>已发布图谱结构不可原地修改；类、属性、关系使用稳定编码供字段映射、知识图谱生成和规则配置引用。<br><br><strong>异常处理：</strong><br>名称为空、编码格式错误或重复时不创建；存在阻断项时保持草稿；已发布或有引用的图谱结构不可删除。 | 页面标题：图谱结构；<br>操作：刷新 / 新建图谱结构 / 编辑 / 查看 / 校验 / 发布 / 复制版本 / 删除；<br>空状态：没有符合条件的图谱结构；<br>提示：已发布版本只读，变更必须复制新版本。 | P0 | 待评审 | 与OntologyLocalPages中的产品化文案、GraphManagementPage入口一致。 |
| 图谱结构编辑器 | 图谱结构列表 → 点击结构名称或“编辑/查看” | [图片] | <strong>页面结构：</strong><br>顶部展示返回、结构编码、名称、状态、版本、范围、最近保存时间，以及“结构化配置/可视化建模”编辑方式。草稿提供保存、校验、发布；已发布提供复制新版本。 图1<br>结构化配置页签为基本信息、类定义、属性、关系、版本记录；可视化建模提供新增类、属性、关系、布局方式和缩放。 图2<br>结构元素抽屉展示元素类型、编码、名称、所属类或关系端点、数据类型/方向、约束和业务说明。 图3<br><br><strong>交互逻辑：</strong><br>1. 用户进入草稿默认使用结构化配置，进入已发布版本默认使用可视化视图；系统记忆最近使用的编辑方式。<br>2. 用户在“类定义”新增业务对象、业务记录或业务概念。 图2<br>3. 用户在“属性”选择所属类并配置数据类型和约束；主标识、必填、发生时间等作为属性约束维护。 图3<br>4. 用户在“关系”选择起点类和终点类，系统自动形成关系方向。 图4<br>5. 用户可在可视化画布拖入类、属性、关系，点击类查看属性和关联关系；新增类时保存画布位置。 图5<br>6. 用户点击保存草稿写入数据；点击校验检查元素编码、所属类和关系端点；待校验版本确认后发布。 图6<br>7. 修改或删除被属性/关系引用的类时，系统阻止操作并要求先调整引用；已发布版本只能查看或复制新版本。<br><br><strong>策略设计：</strong><br>业务发生记录通过类、属性和关系表达，不提供独立事件页签。结构化配置与可视化建模共用同一草稿数据。<br><br><strong>异常处理：</strong><br>元素编码或名称为空、同类型编码重复、所属类/关系端点不存在时拒绝保存或校验；发布条件不满足时保留草稿。 | 编辑方式：结构化配置 / 可视化建模；<br>页签：基本信息 / 类定义 / 属性 / 关系 / 版本记录；<br>操作：保存草稿 / 校验 / 发布 / 复制新版本；<br>提示：已发布版本只读，请先复制新版本。 | P0 | 待评审 | 与OntologyEditorPage和OntologyVisualModeler当前可见页面一致。 |
| 数据源列表 | 一级菜单“知识图谱” → 二级菜单“数据源” | [图片] | <strong>页面结构：</strong><br>页面展示关键词、状态筛选、“新建数据源”和数据源总数、启用数据源、异常数据源、近期处理数据4项指标。列表展示来源名称/编码、适用图谱结构、来源方式、数据范围、责任人、最近成功、状态和操作。 图1<br>新增使用抽屉，删除使用高危确认弹窗。 图2<br><br><strong>交互逻辑：</strong><br>1. 用户进入数据源页面，系统加载数据源、同步记录和已发布图谱结构。<br>2. 用户可按关键词和状态查询或重置。 图1<br>3. 用户点击“新建数据源”，先选择适用图谱结构，再填写来源系统名称、来源方式、责任人和数据范围，点击“保存并继续”后创建草稿并进入详情。 图2<br>4. 用户可对已有数据源执行进入详情、测试、启用/停用、删除。 图3<br>5. 已启用、已被知识图谱引用或已有同步记录的数据源不可删除，需先解除对应约束。 图4<br>6. 用户点击“图谱结构”二级菜单，可回到图谱结构管理页面。 图5<br><br><strong>策略设计：</strong><br>数据源可被多个知识图谱生成结果复用；启用前必须至少存在有效字段映射。<br><br><strong>异常处理：</strong><br>名称、范围或适用图谱结构为空时不创建；删除约束不满足时明确提示阻断原因；加载失败保留筛选并提示重试。 | 页面标题：数据源；<br>操作：新建数据源 / 进入详情 / 测试 / 启用 / 停用 / 删除；<br>空状态：暂无数据源；<br>成功提示：数据源草稿{编号}已创建，请继续测试连接。 | P0 | 待评审 | 与GraphManagementPage内嵌DataAccessPage和DataSourceDetailPage入口一致。 |
| 数据源详情与字段映射 | 数据源列表 → 点击来源名称或“进入详情” | [图片] | <strong>页面结构：</strong><br>顶部展示返回、数据源编号/名称、状态、来源方式、适用结构、责任人、最近成功时间，以及测试连接、启用/停用。页面展示元数据对象、图谱字段、字段映射、必填覆盖4项指标。 图1<br>主体页签为基本配置、字段映射；字段映射表展示图谱字段、字段要求、来源字段、转换规则、状态和操作。 图2<br><br><strong>交互逻辑：</strong><br>1. 用户进入详情，系统并行加载数据源、来源结构、字段映射、图谱结构元素和同步记录。<br>2. 用户点击“测试连接”，成功后更新连接结果；在基本配置页点击“解析来源结构/重新解析来源结构”，系统生成来源结构和字段映射建议。 图3<br>3. 用户进入字段映射页，以图谱字段为主线查看是否已匹配来源字段。 图4<br>4. 用户点击“配置/编辑”，打开字段映射抽屉，选择来源字段并填写转换与识别规则，保存后映射状态变为待校验。 图5<br>5. 用户点击“校验字段映射”，系统校验来源字段、目标图谱字段和转换规则；有效映射数量和必填覆盖同步更新。 图6<br>6. 至少存在有效字段映射后，用户可启用数据源；启用后可被知识图谱生成向导选择。 图7<br>7. 用户点击“返回数据源列表”回到数据源页面。<br><br><strong>策略设计：</strong><br>字段映射只处理来源字段到图谱字段；实例关系在生成知识图谱时根据图谱结构和业务键自动构建。<br><br><strong>异常处理：</strong><br>数据源不存在时展示不可访问；无图谱字段时引导先维护图谱结构；未解析来源结构时展示解析入口；无有效映射时不可启用。 | 页签：基本配置 / 字段映射；<br>操作：测试连接 / 解析来源结构 / 重新解析来源结构 / 配置字段映射 / 保存映射 / 校验字段映射 / 启用 / 停用；<br>提示：修改字段映射会影响后续使用该数据源生成的知识图谱。 | P0 | 待评审 | 与DataSourceDetailPage和SourceMapping当前实现一致。 |
| 知识图谱生成向导 | 独立路由“/graphs/new”或可编辑生成结果“/graphs/:id/edit” | [图片] | <strong>页面结构：</strong><br>向导包含基本信息、图谱结构、数据源与字段映射、确认生成4步。选择图谱结构卡片展示类、属性、关系统计；选择数据源步骤展示映射模板自动检查。 图1<br>映射未就绪时可进入映射维护工作区，表格展示类型、来源字段、转换与识别规则、目标图谱元素和状态。 图2<br><br><strong>交互逻辑：</strong><br>1. 用户填写图谱名称、图谱编码和数据范围，点击下一步。<br>2. 用户选择一个已发布图谱结构；卡片展示类、属性和关系数量。 图1<br>3. 用户选择一个或多个启用数据源，系统检查每个数据源与当前图谱结构的映射状态。<br>4. 映射未就绪时点击“维护映射模板”；修正来源字段、转换规则或目标图谱元素后保存并校验。 图2<br>5. 全部依赖ready后进入确认生成，核对图谱、图谱结构、数据源、映射模板快照和数据范围。<br>6. 新建模式点击“生成知识图谱”，编辑模式点击“保存并重新生成”；系统固化映射快照并生成结果。 图3<br>7. 用户退出存在未保存内容时确认；独立新建页面可保存会话草稿。<br><br><strong>策略设计：</strong><br>生成前强制依赖检查，避免使用未发布图谱结构、停用数据源或未校验字段映射。生成结果统一为节点和关系图谱。<br><br><strong>异常处理：</strong><br>任一步缺项时停留当前步骤；映射目标失效、数据源状态变化或字段映射失效时重新检查并阻止生成。 | 步骤：基本信息 / 图谱结构 / 数据源与字段映射 / 确认生成；<br>操作：保存草稿 / 上一步 / 下一步 / 维护映射模板 / 保存并校验 / 生成知识图谱 / 保存并重新生成；<br>依赖状态：ready / disabled / missing / pending / invalid。 | P1 | 待评审 | 与GraphCreateWizard、dataGraphApi依赖检查和图谱生成接口一致；当前未作为左侧二级菜单独立展示。 |
`

const start = content.indexOf('#### 10.3.5 ')
const end = content.indexOf('\n#### 10.3.6 ', start)
if (start === -1 || end === -1) throw new Error('未找到10.3.5章节边界')
content = `${content.slice(0, start)}${newSection}\n\n${content.slice(end + 1)}`

fs.writeFileSync(targetPath, content, 'utf8')
console.log(targetPath)
