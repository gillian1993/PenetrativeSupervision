import fs from 'node:fs'

const input = 'outputs/穿透式监管产品PRD-v11.17-全局工程口径对齐版.md'
const output = 'outputs/穿透式监管产品PRD-v11.19-上线验收标准重写版.md'

let doc = fs.readFileSync(input, 'utf8')

doc = doc.replace(
  '# 穿透式监管智能应用平台 PRD（v11.17 全局工程口径对齐版）',
  '# 穿透式监管智能应用平台 PRD（v11.19 上线验收标准重写版）',
)
doc = doc.replace('| 当前版本 | v11.17 全局工程口径对齐版 |', '| 当前版本 | v11.19 上线验收标准重写版 |')
doc = doc.replace(
  '| 2026-07-27 | 全局清理旧语义资产口径，补齐系统管理模型管理页面，重写10.4平台图谱结构框架，并修正工作台默认入口描述 | 10.3.5已不再使用旧概念，其他章节需同步与当前工程菜单、路由和页面文案保持一致 | [TODO] | [TODO] | v11.17 |',
  '| 2026-07-27 | 重写4.2上线验收标准，以当前工程功能链路为验收对象，补充上线准入、模块验收、数据权限、证据快照、性能与回滚口径 | 上一版验收标准偏项目交付检查，需要回到PRD的产品可用性、业务闭环和用户验收标准 | [TODO] | [TODO] | v11.19 |\n| 2026-07-27 | 全局清理旧语义资产口径，补齐系统管理模型管理页面，重写10.4平台图谱结构框架，并修正工作台默认入口描述 | 10.3.5已不再使用旧概念，其他章节需同步与当前工程菜单、路由和页面文案保持一致 | [TODO] | [TODO] | v11.17 |',
)

const section = `### 4.2 上线验收标准

上线验收判断的是“当前版本是否可以进入试点/生产使用”，不是单纯判断页面是否开发完成。验收应以当前工程已实现的监管工作台、监管态势、风险监管、场景与规则、知识图谱、系统管理六类监管功能为对象，重点验证业务闭环是否可用、数据与证据是否可信、权限是否真实生效、异常场景是否不会产生错误监管结论。

**1. 上线准入标准**

1. 本期上线范围已冻结，包含上线组织、用户角色、监管领域、试点场景、来源系统、目标业务记录和数据时间范围。
2. 当前工程可见路由均可正常访问，一级菜单、二级菜单、列表页、详情页、编辑页、抽屉和弹窗无白屏、无阻断性前端报错。
3. 图谱结构、数据源、字段映射、知识图谱、规则资产、Skill资产、风险场景版本均完成初始化，并具备可追溯的版本编号。
4. 预警、风险事件、待办、消息、整改、复核、权限和审计的正式状态不得只依赖浏览器本地状态；刷新页面、换浏览器、换账号后状态应保持一致。
5. 服务端完成菜单、按钮、组织范围、字段、关系、证据和高危操作权限校验，前端隐藏不得作为唯一权限控制手段。
6. 上线前至少跑通一条端到端闭环：知识图谱准备 → 规则/Skill与场景发布 → 预警生成 → 研判处置 → 风险事件整改 → 监管复核关闭 → 审计追溯。

**2. 模块上线验收标准**

| 模块 | 验收对象 | 上线验收标准 | 不通过判定 |
| --- | --- | --- | --- |
| 监管工作台 | 统计卡、我的待办、监管通知 | 统计卡与待办列表口径一致；待办和通知仅展示当前用户有权处理或查看的数据；点击来源可跳转到正确页面并恢复上下文 | 待办数量与列表不一致；越权事项可见；消息无法跳转 |
| 监管态势 | 指标卡、趋势图、组织/场景分布、下钻 | 大盘指标与统一预警、风险事件数据同源；筛选后图表和下钻列表一致；无权限组织和对象不可见 | 图表和下钻口径不一致；筛选失效；展示未授权数据 |
| 统一预警 | 预警列表、预警详情、转派、解除、升级 | 预警状态仅按“待研判、已解除、已升级”流转；转派、解除、升级均校验必填项、角色权限和重复提交；同一预警最多升级为一个风险事件 | 重复升级生成多个事件；无权解除重大/高风险预警；处置后待办不同步 |
| 风险事件 | 风险事件列表、详情、整改、复核、关闭 | 风险事件状态按“待整改、待复核、已关闭”闭环；整改材料、复核证据、退回原因按规则校验；关闭后只读保留 | 已关闭事件仍可处置；证明材料缺失仍提交；状态并发覆盖 |
| 场景与规则 | 风险场景、规则管理、Skill管理 | 场景作为统一发布单元；至少绑定一项启用规则或Skill；校验、试跑、发布依赖快照一致；发布后场景、规则版本、Skill版本只读 | 未试跑即可发布；依赖变化后仍允许发布；已发布版本可被原地修改 |
| 知识图谱 | 图谱结构、数据源、字段映射、知识图谱生成 | 已发布图谱结构只读；数据源启用前必须有有效字段映射；生成知识图谱时固化图谱结构、数据源和字段映射快照；发布后可被规则引用 | 未发布结构被引用；无有效映射仍启用数据源；生成结果缺少版本快照 |
| 系统管理 | 用户与组织、角色与权限、模型管理、审计日志 | 用户停用前完成未办任务转派；权限保存后影响菜单、按钮、字段、关系和证据访问；模型目录支持接入、试问、绑定场景和启停；审计日志只读 | 停用用户后仍有待办；权限仅前端生效；审计可编辑或删除 |

**3. 数据与证据验收标准**

1. 来源数据需标明来源系统、同步时间、数据范围、责任人和最近处理结果。
2. 字段映射需覆盖试点场景所需的关键字段；阻断项为0，非阻断问题需有处理说明。
3. 预警详情中的规则、知识图谱版本、图谱结构版本、字段映射快照、实际证据和来源记录必须可追溯。
4. 证据快照一经预警生成不得被当前知识图谱覆盖；刷新页面只能刷新状态、任务和权限，不能替换历史证据事实。
5. 附件或来源记录失效时保留元数据、哈希、失效原因和补充入口，不得补造证据或生成无依据结论。

**4. 权限与安全验收标准**

1. 使用至少三个角色完成验收：监管负责人、领域监管专员、业务责任人；如涉及系统配置，还需加入配置管理员和安全/系统管理员。
2. 未授权用户不得访问受限菜单、详情路由、字段明文、证据详情、关系详情和高危操作接口。
3. 解除重大/高风险预警、升级风险事件、复核关闭、发布场景/规则/图谱结构、停用用户、调整权限等高危操作必须二次确认、填写原因并写入审计。
4. 审计记录必须包含操作人、组织、角色、时间、对象类型、对象编号、操作结果、原因、IP和trace_id；审计详情只读，不允许业务页面覆盖或删除。

**5. 性能与稳定性验收标准**

| 验收项 | 上线标准 |
| --- | --- |
| 普通列表查询 | 默认授权范围下P95不超过2秒 |
| 监管态势首屏 | 默认统计范围P95不超过5秒 |
| 预警详情非图谱信息 | P95不超过3秒 |
| 证据子图 | 30个节点、50条关系以内P95不超过5秒 |
| 异步预警生成 | 业务记录到达后P95不超过60秒 |
| 高危操作幂等 | 重复提交不产生重复预警、风险事件、整改或复核记录 |
| 可用性 | 试点期核心服务可用性不低于99.5% |
| 备份恢复 | RPO不超过24小时，RTO不超过4小时 |

**6. 上线判定口径**

1. P0验收项必须100%通过；任何影响预警研判、风险事件闭环、权限安全、证据快照真实性的问题均视为P0阻断。
2. P1验收项通过率不低于95%；未通过项必须具备临时规避方案、负责人和关闭时间，不影响试点核心闭环。
3. P2问题可进入上线遗留清单，但不得影响核心路径、权限边界、审计追溯和数据可信度。
4. 验收结论分为“通过、条件通过、不通过”。条件通过只允许限定组织、限定场景、限定数据范围上线。
5. 上线后设置两周观察期；观察期出现P0问题时，立即停用相关场景版本或回退上一规则/知识图谱版本，并保留故障期间审计记录。

**7. 验收材料**

上线验收材料至少包括：上线范围确认单、部署记录、冒烟测试记录、端到端闭环用例记录、数据质量报告、权限测试记录、证据快照抽查记录、审计抽查记录、性能测试记录、备份恢复演练记录、上线遗留问题清单和回滚方案。
`

doc = doc.replace(/### 4\.2 交付验收标准[\s\S]*?(?=### 4\.3 试运行成功标准)/, section + '\n\n')

doc = doc.replace(
  '| AC-12 | 试点准入 | 第4.2节试点条件满足后方可真实数据试运行 |',
  '| AC-12 | 试点准入 | 第4.2节上线验收准入标准满足后方可真实数据试运行 |\n| AC-13 | 上线判定 | P0验收项100%通过，P1遗留项有规避方案、负责人和关闭时间 |',
)

doc = doc.replace(
  '| TBD-9 | 确认商业目标、价格和试运行指标目标值 | [TODO] | 试点启动前 |',
  '| TBD-9 | 确认商业目标、价格和试运行指标目标值 | [TODO] | 试点启动前 |\n| TBD-10 | 确认上线验收材料模板、验收签字人和两周观察期值班安排 | [TODO] | 上线验收前 |',
)

fs.writeFileSync(output, doc, 'utf8')
console.log(output)
